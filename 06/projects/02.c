#include <stdio.h>

int main(void)
{
    int m, n;
    int gcd;
    int remainder;

    printf("Enter two integers separated by a space: ");
    scanf("%d %d", &m, &n);

    // *** Use Euclid's algorithm ***

    return 0;
}
